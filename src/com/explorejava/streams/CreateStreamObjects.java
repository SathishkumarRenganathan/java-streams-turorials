package com.explorejava.streams;import java.util.*;import java.util.stream.Stream;public class CreateStreamObjects {    public static void main(String args[]) {        System.out.println("Success!!");        //create a stream        Stream<String> streamObj = Stream.of("a", "b", "c");        streamObj.forEach(System.out::println);        //creating a stream from various sources like collection        Collection<String> collectionObj = Arrays.asList("Test1", "Test2", "Test3");        Stream<String> streamObj2 = collectionObj.stream();        streamObj2.forEach(System.out::println);        //creating a stream from various sources like List        List<String> listObj = Arrays.asList("Java", "J2EE", "Spring");        Stream<String> streamObj3 = listObj.stream();        streamObj3.forEach(System.out::println);        //creating a stream from various sources like Set        Set<String> setObj = new HashSet<String>(listObj);        Stream<String> streamObj4 = setObj.stream();        streamObj4.forEach(System.out::println);        //creating a stream from various sources like Array        String[] arrayObj = {"a", "b", "c"};        Stream<String> streamObj5 = Arrays.stream(arrayObj);        streamObj5.forEach(System.out::println);    }}